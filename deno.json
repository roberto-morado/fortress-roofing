{
  "tasks": {
    "start": "deno run -A main.ts",
    "check": "deno check main.ts",
    "build:static-optional": "echo 'DEPRECATED: static export is not the product. Use deno task start and deploy to Deno Deploy. See README.md'"
  },
  "compilerOptions": {
    "strict": true,
    "lib": ["deno.window"]
  }
}
